# 质检进度 — {书名}

> 职责：逐章记录 Phase 5 质量检查各子项的完成状态，机械可查，不靠翻 追踪/上下文.md 的叙述性日志找答案。
> 每写完一章，写作流程收尾时更新对应行；不确定某项是否做过时，以本表为准，没打勾就是没做过。
> 完整发现记录仍写回 追踪/上下文.md「最近决策」；本表只管"跑没跑"，不管"跑出了什么"。

## 图例

- `✓` 已完成且通过（blocking 清零 / 无 S1-S2 未解决问题）
- `○` 已跑但有未解决的 advisory/S3 待复核
- `—` 未跑（新章节默认状态）
- `n/a` 本章不适用（例如无新增伏笔时 consistency-checker 可标 n/a，但去AI味审查不因此免跑）

## 逐章质检表

| 章号 | check-typos.js | check-ai-patterns.js | check-degeneration.js | normalize-punctuation.js | 元信息扫描 | consistency-checker | 去AI味独立审查 | 字数核实（脚本非估算） | 对话密度实测 |
|---|---|---|---|---|---|---|---|---|---|
| 第001章 | — | — | — | — | — | — | — | — | — |

> 新增一章即在表格底部加一行；不要用文字描述代替表格状态。

## 使用说明

- **consistency-checker** 和 **去AI味独立审查** 是长篇写作流程 Phase 5 的硬性必须项，不是"如果部署了就跑"的可选项；agent 未部署时改为主线程按 `quality-checklist.md` / `anti-ai-writing.md` 手动执行，同样要在本表打勾，不能因为改成手动执行就跳过记录。
- 每 5 章做一次批量核对：检查本表是否有连续多行同一列是 `—`，出现即说明该项被系统性漏跑，需要立即补跑，不要等到发布前才做一次性终审。
- 字数核实必须来自脚本/Python 精确统计，agent 自己的"人工估算"不算数，标 `○` 并在 上下文.md 备注需要主会话补跑。
