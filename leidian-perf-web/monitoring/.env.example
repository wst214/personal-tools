# 复制为 .env 后按需修改（可选）
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# 默认用官方镜像名，由 Docker Desktop registry-mirrors 加速
# 若仍慢，可改为直连镜像站（二选一）：
# PROMETHEUS_IMAGE=docker.1ms.run/prom/prometheus:v3.2.1
# GRAFANA_IMAGE=docker.1ms.run/grafana/grafana:11.5.2
