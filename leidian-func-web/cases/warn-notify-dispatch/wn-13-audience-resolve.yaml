id: wn-13-audience-resolve
name: 通知对象应按策略解析出接收人
description: 【暂不执行】验收：通知受众应按策略解析到接收人。
skip: true
skipReason: 能力未上线；本用例按验收标准编写，上线后取消 skip 即可跑
steps:
- method: GET
  service: biz
  path: /warning/notify/records?pageNum=1&pageSize=1
  expect:
    status: 200
    fields:
      code: 0
  capture:
    recordId: data.list.0.id
- method: GET
  service: biz
  path: /warning/notify/records/${recordId}
  expect:
    status: 200
    fields:
      code: 0
