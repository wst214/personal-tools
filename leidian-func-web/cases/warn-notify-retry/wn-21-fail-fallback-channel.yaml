id: wn-21-fail-fallback-channel
name: 失败后可切换备用通道
description: 【暂不执行】验收：失败后可切换备用通道。
skip: true
skipReason: 能力未上线；本用例按验收标准编写，上线后取消 skip 即可跑
steps:
- method: GET
  service: biz
  path: /warning/notify/records?pageNum=1&pageSize=20
  expect:
    status: 200
    fields:
      code: 0
